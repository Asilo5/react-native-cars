import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const Header = () => {
  return (
    <View>
      <Text> Cars </Text>
    </View>
  );
};

// const styles = StyleSheet.create({

// });

export default Header;
